name: CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Simulate Deployment
        run: echo "Deploying app..."

      - name: Use Secret
        run: echo "Secret is ${{ secrets.DEPLOY_KEY }}"
